# Чеклист прототипа ICAT Grid

- [x] До текущих правок исходное состояние зафиксировано коммитом `da801a4` и отправлено в `YuriyBevov/imp-cat` (`main`).
- [x] Создана отдельная папка `icat-test`, не связанная с рабочим приложением.
- [x] Настроен локальный Node.js/Express-сервер без внешних API.
- [x] Добавлена загрузка DOCX через выбор файла и drag-and-drop.
- [x] Добавлен встроенный тестовый DOCX.
- [x] Реализован визуальный рендер страниц DOCX в браузере.
- [x] Добавлен выбор логического шага перемещения/resize 1–96 px для текущего документа.
- [x] Координатная сетка возвращена только внутрь белых листов; серый фон интерфейса и буфер вне документа остаются без сетки.
- [x] Текстовая область Word между полями выделена отдельным бордером и не обозначается как граница обрезки.
- [x] Добавлен независимый плавный экранный масштаб 25–250% с корректным пересчётом drag/resize.
- [x] Добавлен зум под курсором через `Cmd/Ctrl + колесо мыши`.
- [x] Зум пересчитывает прокрутку по геометрии до/после масштабирования и удерживает точку документа под курсором как в Figma.
- [x] Добавлен режим автоматического вписывания документа по ширине поля.
- [x] Неоднозначная настройка «Высота рабочего поля» и служебные линии виртуальных Word-страниц удалены.
- [x] Страницы исходного DOCX сохраняются по Word-пагинации `docx-preview`; ненадёжное повторное разбиение по общему `scrollHeight` удалено.
- [x] Физические размеры листа берутся из объявленных Word `width/min-height`, а не из увеличенного `scrollHeight` контента.
- [x] Обычный текстовый поток, вышедший за физическую высоту листа, переносится на отдельные страницы продолжения без прижатия всех блоков к одной нижней координате.
- [x] Потоковые абзацы раскладываются последовательно: перенос одного блока за нижнее поле Word сдвигает все следующие блоки и не позволяет им наложиться на контент следующей страницы.
- [x] Плавающие фигуры, печати и абсолютно позиционированные элементы исключены из расчёта страниц продолжения и не создают ложных листов.
- [x] После каждой страницы добавлена команда вставки пустого листа, включая вставку между существующими страницами.
- [x] Вставка пустой страницы добавлена в общую историю и отменяется через `Cmd/Ctrl + Z` с восстановлением нумерации.
- [x] Добавлено удаление страницы без активных сегментов; последний лист и непустые страницы защищены от удаления.
- [x] Удаление пустой страницы перенумеровывает ссылки сегментов и отменяется через `Cmd/Ctrl + Z`.
- [x] Реализованы детерминированные ID ячеек `P{page}:R{row}:C{column}`.
- [x] Текст документа извлекается в отдельные визуальные сегменты.
- [x] Плавающая Word-фигура с несколькими вложенными абзацами импортируется как один сегмент.
- [x] Родительские абзацы, содержащие плавающие фигуры, больше не создают дублирующие сегменты.
- [x] Прямой текст смешанного Word-абзаца больше не теряется из-за соседней плавающей фигуры: заголовки вроде «ДОВЕРЕННОСТЬ» сегментируются отдельно, а текст фигуры не дублируется.
- [x] Обычные и смешанные текстовые сегменты занимают всю ширину контентной области; таблицы и плавающие фигуры сохраняют собственную ширину.
- [x] Исправлено измерение сегментов: страницы участвуют в layout до чтения координат.
- [x] Убрано округление исходных размеров и принудительный минимум 96 × 32 px, вызывавшие наложения в плотных таблицах.
- [x] Наведённый и выбранный сегменты поднимаются поверх соседей для доступа к управлению.
- [x] Сегменты редактируются непосредственно на странице.
- [x] Исходные Word-run сохраняют жирность, курсив и цветную подсветку; скрытый исходный слой больше не оставляет пустые жёлтые прямоугольники.
- [x] Специальные Word-табуляторы сохраняются как позиционные интервалы, поэтому строки подписей и штампов не схлопываются в одну строку.
- [x] В сегмент добавлена WYSIWYG-панель: полужирный, курсив и выравнивание по левому краю, центру или правому краю.
- [x] WYSIWYG-форматирование сохраняется в истории `Cmd/Ctrl + Z` и в экспортируемых Word-run.
- [x] Реализовано перемещение сегментов с логическим шагом привязки.
- [x] Реализовано рамочное выделение нескольких сегментов левой кнопкой мыши.
- [x] Реализовано групповое перемещение выбранных сегментов внутри листа и между листами с сохранением относительного положения.
- [x] Буфер «Вне документа» перенесён в липкую правую колонку рядом со страницами и расширяется по ширине/высоте под переносимую группу.
- [x] Размер правого буфера вручную изменяется за нижний правый угол; выбранная пользователем высота не уменьшается автоматически.
- [x] Вынесенным сегментам назначаются `pageIndex/pageId = null`; инспектор показывает отсутствие координат страницы.
- [x] Вынесенные сегменты исключаются из экспорта, счётчика активных сегментов и поиска наложений до возврата на лист.
- [x] Единый unit-тестируемый предикат документного сегмента отклоняет удалённые, припаркованные и потерявшие индекс страницы блоки.
- [x] Возврат из буфера перетаскиванием снова назначает страницу и координаты; перенос в/из буфера записывается в историю отмены.
- [x] Возврат из буфера выполняется перетаскиванием; лишние команды соседних страниц удалены из меню.
- [x] Реализовано изменение размера сегментов с логическим шагом привязки.
- [x] Добавлены удаление, сброс и перенос между страницами.
- [x] Добавлена пошаговая отмена до 100 операций через `Cmd/Ctrl + Z` для текста, drag/resize, групповых перемещений, выноса из документа, удаления, сброса, переноса между страницами и разнесения наложений.
- [x] Выпадающее меню активного сегмента поднимается поверх остальных блоков.
- [x] В меню оставлены «Вернуть в исходное положение в документе» и «Удалить сегмент».
- [x] Действие меню выбранного сегмента применяется ко всей выделенной группе и записывается в историю одной операцией.
- [x] Клик вне сегментов и `Esc` закрывают меню, снимают DOM-фокус и визуальное выделение.
- [x] После успешной загрузки скрываются загрузочная область и верхняя кнопка загрузки; сервер и БД для этого не используются.
- [x] Добавлены счётчик наложений и управляемая команда вертикального разнесения.
- [x] В инспекторе выводятся ID, страница, ячейка, координаты и размер.
- [x] Реализована серверная проверка экспортируемой координатной модели.
- [x] Реализована сборка редактируемого DOCX из позиционированных текстовых блоков.
- [x] Экспорт использует фактический список исходных и вручную вставленных пустых страниц.
- [x] Экспорт переведён на независимые совместимые VML-блоки `w:pict/v:shape`, закреплённые относительно страницы.
- [x] Из экспортируемого результата исключены WPS/DrawingML и legacy `framePr`, вызывавшие ошибки или смешение координат.
- [x] Исправлены служебные инварианты VML: `shapetype` определяется один раз, VML-ID уникальны, `spidmax` превышает последний ID.
- [x] Каждый `w:pict/v:shape` вынесен из общего XML-абзаца в собственный минимальный якорный абзац, чтобы плотный экспорт не зависел от одного перегруженного контейнера.
- [x] Исправлена причина ошибки Word на пользовательском четырёхстраничном экспорте: CSS-выравнивание `justify` преобразуется в допустимое WordprocessingML-значение `both`.
- [x] После сборки сервер повторно проверяет ZIP, все XML-части, количество страниц/сегментов, допустимые выравнивания и VML-инварианты до скачивания.
- [x] Добавлены unit-тесты валидации координат и ID ячеек.
- [x] Добавлены unit-тесты истории: пропуск пустых действий, порядок отмены, лимит 100 операций и очистка.
- [x] Добавлены тесты двухстраничного DOCX, пустой средней страницы и стресс-тест 240 независимых плавающих сегментов.
- [x] Добавлен unit-тест приоритета физической Word-высоты над переполненной DOM-высотой.
- [x] Добавлен регрессионный тест распределения переполненного потока по страницам с учётом верхнего поля Word.
- [x] Добавлены регрессионные тесты последовательного переноса, Word-табуляторов, run-level жирности/курсива/подсветки и их DOCX-экспорта.
- [x] Drag ограничен одним обновлением на кадр; геометрия поверхностей измеряется один раз в начале переноса, а DOM-слой меняется только при переходе на другой лист.
- [x] Поиск наложений ускорен постраничным пространственным индексом вместо полного попарного перебора.
- [x] Добавлен тест полной ширины контента для абзацев с сохранением геометрии плавающих фигур.
- [x] Добавлены DOM-тесты смешанного абзаца с прямым текстом и SVG-фигурой.
- [x] Добавлен многостраничный регрессионный тест преобразования `justify` → `both`.
- [x] Повторно выполнен интеграционный HTTP smoke-тест: `/api/health` отвечает `200`, экспорт создаёт валидный двухсекционный DOCX с двумя VML-блоками.
- [x] Экспортированный DOCX повторно прочитан и проверен: текст, секции, VML-блоки и абсолютные координаты сохранены.
- [x] Исправленная копия реального четырёхстраничного 73-сегментного DOCX прошла ZIP/XML/VML-проверку и открыта настольным Microsoft Word (`Success:true`, `Result.Code:0`).
- [x] Дополнительный стресс-DOCX с 240 сегментами открыт настольным Microsoft Word (`Success:true`, `Result.Code:0`).
- [x] Интеграционный DOCX, полученный через `POST /api/export`, открыт настольным Microsoft Word (`Success:true`, `Result.Code:0`).
- [x] Подготовлена инструкция локального запуска и список ограничений.
- [x] Подготовлено отдельное пошаговое описание архитектуры, инструментов и выполненных работ в `HOW_IT_WORKS.md`.
- [x] Проведена ревизия файлов проекта; удалена неиспользуемая фикстура со старой схемой `fixtures/export-smoke.json`.
- [ ] Интерактивный UI smoke-тест в браузере — браузер недоступен в текущей среде; требуется ручная проверка.
- [ ] Ручная проверка последовательности из нескольких `Cmd/Ctrl + Z` в браузере — браузерный канал автоматизации недоступен; сценарий добавлен в отчёт.
- [ ] Сохранение изображений и сложных объектов в экспортируемом DOCX — следующий этап.
- [ ] Редактирование исходного OOXML вместо сборки нового документа — следующий этап.
- [ ] Визуальная регрессия «браузер → DOCX/PDF» — следующий этап.
