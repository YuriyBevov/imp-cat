{
  "name": "icat-grid-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "ICAT hypothesis prototype: DOCX preview, grid-based segment editing and positioned DOCX export",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch server.cjs",
    "start": "node --env-file-if-exists=.env server.cjs",
    "onlyoffice:up": "docker compose -f docker-compose.onlyoffice.yml up -d",
    "onlyoffice:down": "docker compose -f docker-compose.onlyoffice.yml down",
    "onlyoffice:logs": "docker compose -f docker-compose.onlyoffice.yml logs -f document-server",
    "test": "node --test tests/*.test.cjs",
    "test:python": ".venv/bin/python -m unittest discover -s tests -p 'test_*.py'",
    "check": "node --check server.cjs && node --check lib/onlyoffice.cjs && node --check lib/credential-vault.cjs && node --check lib/job-manager.cjs && node --check lib/document-agent.cjs && node --check lib/studio.cjs && node --check lib/studio-model.cjs && node --check public/layout-model.js && node --check public/history-model.js && node --check public/ooxml-model.js && node --check public/segmentation-model.js && node --check public/app.js && node --check public/onlyoffice.js && node --check public/studio.js && node -e \"JSON.parse(require('node:fs').readFileSync('schemas/document-analysis.schema.json'))\" && .venv/bin/python -m py_compile scripts/document_analyzer.py scripts/export_docx.py scripts/export_studio.py && npm test && npm run test:python"
  },
  "dependencies": {
    "docx-preview": "0.4.0",
    "express": "5.2.1"
  },
  "engines": {
    "node": ">=22.12"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
