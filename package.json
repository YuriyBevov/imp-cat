{
  "name": "icat-grid-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "ICAT hypothesis prototype: DOCX preview, grid-based segment editing and positioned DOCX export",
  "scripts": {
    "dev": "node --watch server.cjs",
    "start": "node server.cjs",
    "test": "node --test tests/*.test.cjs",
    "test:python": ".venv/bin/python -m unittest discover -s tests -p 'test_*.py'",
    "check": "node --check server.cjs && node --check public/layout-model.js && node --check public/app.js && npm test && npm run test:python"
  },
  "dependencies": {
    "docx-preview": "0.4.0",
    "express": "5.2.1"
  },
  "engines": {
    "node": ">=22.12"
  }
}
