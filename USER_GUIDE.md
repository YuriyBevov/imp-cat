# Руководство пользователя ICAT Translation Studio

## 1. Назначение приложения

ICAT Translation Studio — рабочее место бюро переводов для распознавания, перевода, редактирования и сборки документов. В текущей версии можно:

- загрузить PDF, скан или изображение;
- распознать содержимое документа с помощью выбранной AI-модели;
- получить текстовые сегменты с координатами и типами объектов;
- сопоставить исходник и перевод в двух колонках;
- использовать переводческую Базу знаний;
- перевести отдельные сегменты, выбранную группу или весь документ;
- уточнить перевод для AI общим или посегментным комментарием;
- вручную исправить текст, типографику и расположение объектов;
- проверить документ и скачать результат в DOCX или PDF.

Поддерживаемые входные форматы: PDF, PNG, JPG/JPEG, WebP, TIFF, HEIC и BMP. Поддерживаемые форматы выгрузки: DOCX и PDF.

## 2. Рекомендуемый порядок работы

1. Откройте ICAT в браузере.
2. В разделе «AI-провайдер» выберите AITunnel или Codex и проверьте подключение.
3. Загрузите один или несколько документов.
4. Дождитесь окончания распознавания. Не закрывайте вкладку, если хотите видеть текущий прогресс; само задание продолжает работать на сервере.
5. После распознавания документ по умолчанию открывается в основной рабочей области «Макет» и масштабе 100%, а панель оригинала скрыта. При необходимости раскройте оригинал кнопкой с иконкой листа-макета и сравните с ним распознанный текст.
6. Исправьте пропущенный или неверно распознанный текст и типы специальных объектов.
7. Выберите языки исходника и перевода.
8. Выделите необходимые сегменты непосредственно на макете.
9. Запустите перевод выбранных сегментов или всего документа.
10. Проверьте предложения Базы знаний и варианты AI.
11. При необходимости добавьте комментарий для AI и повторно исправьте перевод.
12. Отредактируйте расположение, размеры и оформление сегментов.
13. Запустите сравнение макета с оригиналом и финальную проверку.
14. Скачайте DOCX или PDF.

## 3. Верхняя панель

Компактная верхняя панель имеет высоту 44 пикселя. Кнопка с тремя горизонтальными линиями размером 30 × 30 пикселей в её правой части открывает меню действий. Меню закрывается после выбора команды, клика вне него или нажатия `Esc`.

| Кнопка | Что делает |
| --- | --- |
| **Документы** | Открывает список всех рабочих и архивных документов. |
| **База знаний** | Открывает общий справочник утверждённых пар «исходный текст → перевод». |
| **AI-инструкции** | Открывает отдельное окно управления готовыми правилами перевода. Оно доступно даже без открытого документа. |
| **AI-провайдер** | Открывает выбор маршрута AI, модели и настройку ключа AITunnel. |
| **Документация** | Открывает в новой вкладке актуальные технические требования, архитектуру и критерии приёмки. |
| **Руководство** | Открывает в новой вкладке настоящее пользовательское руководство. |
| **Компоненты** | Открывает каталог согласованных UI-компонентов и используемых SVG-иконок. |
| **Отменить** | Отменяет последнее редактирующее действие. Аналог `Ctrl/Cmd + Z`. |
| **Повторить** | Возвращает отменённое действие. Аналог `Ctrl/Cmd + Shift + Z`. |
| **Другой документ** | Сохраняет текущую сцену и открывает выбор новых файлов. |
| **Скачать DOCX** | Собирает текущую утверждённую сцену в документ Microsoft Word. |
| **Скачать PDF** | Собирает текущую утверждённую сцену в PDF. |

Страницы справки формируются непосредственно из актуальных Markdown-файлов проекта. После обновления руководства или технической документации достаточно перезагрузить соответствующую страницу браузера.

Все пиктограммы интерфейса берутся из локального SVG-набора и сопровождаются текстовой подсказкой или доступным названием. Страница «Компоненты» служит эталоном для уже согласованных элементов; первым в ней зафиксирован блок `SourcePreviewControls`.

## 4. Загрузка и обработка

| Элемент | Что делает |
| --- | --- |
| **Выбрать документы** | Открывает системный выбор файлов. Можно выбрать несколько документов. |
| Область загрузки | Принимает файлы перетаскиванием мышью. |
| Вкладка документа | Переключает текущий рабочий документ. Цветная точка показывает состояние обработки. |
| Кнопка с иконкой закрытия во вкладке | Закрывает вкладку в интерфейсе, но не удаляет сохранённый документ. |

Открытые вкладки собраны у правого края строки документов. Если вкладок больше доступной ширины, список прокручивается по горизонтали.
| **Отменить обработку** | Прерывает выполняющееся AI-задание. Уже сохранённый исходник остаётся на сервере. |
| **Повторить обработку** | Повторно запускает завершившееся с ошибкой или отменённое задание без новой загрузки файла. |

Большие документы делятся сервером на пакеты страниц. Интерфейс показывает общий процент, номер текущего пакета, количество обработанных страниц и найденных сегментов.

## 5. Оригинал документа

Оригинал находится слева от редактируемой области и не меняется при редактировании. При открытии документа панель оригинала по умолчанию скрыта.

Кнопки масштаба находятся поверх оригинала в правом нижнем углу области просмотра.

Слева от оригинала расположена компактная панель страниц шириной 60 пикселей. Каждая миниатюра содержит изображение страницы и её номер снизу; нажатие переводит оригинал и рабочий макет к выбранной странице.

| Кнопка | Что делает |
| --- | --- |
| **−** | Уменьшает оригинал. |
| **+** | Увеличивает оригинал. |
| **100%** | Показывает оригинал в масштабе 1:1. |
| Кнопка с иконкой рамки | Вписывает оригинал по ширине доступной панели. |
| Кнопка с четырьмя угловыми стрелками | Открывает оригинал поверх приложения в полноэкранном просмотре. В нём доступны переход между страницами, отдельный масштаб, режим 100% и вписывание страницы. `Esc` или крестик закрывают просмотр. |
| Кнопка с иконкой листа-макета | Сворачивает или возвращает оригинал, освобождая место для редактора. Активный вид кнопки означает, что оригинал показан; подсказка сообщает текущее действие. |
| Перетаскивание страницы левой кнопкой мыши | Перемещает увеличенный оригинал по горизонтали и вертикали внутри области просмотра. |

`Ctrl/Cmd + колесо мыши` над оригиналом изменяет его масштаб независимо от рабочего документа.

## 6. Рабочая область

Во всём интерфейсе используется единый тонкий скроллбар по образцу briefing-app: дорожка прозрачная, ползунок серый и скруглённый, а при наведении становится темнее. Оформление действует в рабочем холсте, боковых панелях, списках, модальных окнах, документации и других прокручиваемых областях.

Общая панель инструментов располагается сразу под вкладками документов и занимает всю ширину редактора. Она разделена на три визуальные зоны: слева открывается оригинал, по центру находятся масштаб и сетка документа, справа открывается панель рабочих инструментов. Центральная группа не смещается при разной ширине боковых панелей. Рабочая область одна — «Макет» с сегментами в координатах документа; переключателя видов и отдельного парного списка сегментов нет.

Кнопка показа оригинала является частью общей `workbench-toolbar`, но занимает её левую ячейку шириной 60 пикселей непосредственно над миниатюрами страниц. Оригинал раскрывается рядом с панелью страниц слева направо и плавно сужает рабочий холст; повторное нажатие скрывает его в обратном направлении. Во время анимации оригинал проходит под непрозрачной панелью страниц и не перекрывает миниатюры. Сама панель оригинала сделана компактнее, чтобы оставлять больше места редактируемому макету.

В правой панели расположен единый инспектор с инструментами общего перевода, редактирования макета и работы с выбранным сегментом.

Инспектор выезжает справа налево внутри контентной области под общей панелью инструментов. Пока он открыт, рабочий холст сужается на его ширину, поэтому панель не перекрывает документ. У инспектора нет отдельной шапки: кнопка с иконкой правой колонки на панели инструментов открывает или скрывает его, а `Esc` закрывает. На узких экранах инспектор открывается поверх документа, но также остаётся в границах контентной области.

Над аккордеонами при отсутствии выделения показывается компактное уведомление **«Выберите сегмент для работы с ним»**. Все разделы остаются видимыми и раскрытыми; заголовки можно независимо сворачивать и раскрывать, но содержимое трёх разделов выбранного объекта до выбора сегмента приглушено и недоступно для ввода. После выбора сегмента уведомление исчезает и содержимое становится активным; снятие всего выбора возвращает уведомление.

| Кнопка | Что делает |
| --- | --- |
| **− / +** | Уменьшает или увеличивает редактируемый документ. |
| Кнопка с двумя разнонаправленными стрелками | Вписывает рабочий документ по доступной ширине. |
| **100%** | Возвращает редактируемый документ к фактическому масштабу 100%; с этого масштаба документ открывается по умолчанию. |

`Ctrl/Cmd + колесо мыши` меняет масштаб относительно позиции курсора. Масштаб влияет только на отображение и не меняет координаты экспорта.

Выпадающие меню сохраняют минимальную экранную ширину 180 px при уменьшении масштаба документа. Если места у края окна меньше, меню ограничивается доступной шириной, а длинные названия переносятся внутри пункта без горизонтальной прокрутки.

### Добавление и удаление страниц

Под каждым листом находятся две команды:

| Кнопка | Что делает |
| --- | --- |
| Кнопка `+` без текста | Вставляет новый лист того же формата сразу после выбранной страницы. Полное название команды доступно во всплывающей подсказке и скринридеру. Исходные изображения следующих страниц сохраняют правильную привязку. |
| Кнопка с корзиной без текста | Удаляет лист, только если на нём нет включённых в документ сегментов. Полное название и причина недоступности показаны в подсказке. В документе всегда остаётся хотя бы одна страница. |

У добавленной страницы нет изображения оригинала: слева отображается нейтральная пустая страница. Такой лист полноценно участвует в редактировании и экспорте.

## 7. Выбор и перемещение сегментов

- Обычный клик выбирает один сегмент.
- Заголовок инспектора размером 12 px показывает единый счётчик выбора: «Выбран 1 сегмент» или «Выбрано N сегментов» с правильной формой слова. Число выделено красным; отдельной подписи «Выделение» и дублирующего числового индикатора нет.
- `Ctrl/Cmd + клик` добавляет сегмент к текущему выделению или исключает его из выделения.
- Протягивание рамки по свободной области выделяет все пересекающиеся сегменты.
- Перетаскивание выбранного сегмента перемещает всю выделенную группу.
- Во время переноса сегмент свободно следует за курсором и не застревает у границы исходного листа. Целевая страница подсвечивается; после отпускания сегмент переносится в её систему координат и привязывается к сетке. Отпускание в промежутке между листами выбирает ближайшую страницу.
- Выделение не добавляет сегменту отдельную обводку: состояние обозначают только маркеры перемещения и изменения размера, чтобы рамка не мешала точной визуальной расстановке.
- При множественном выделении сегмент в текущем фокусе обозначается синими маркерами перемещения и размера. У остальных выбранных сегментов маркеры серые и менее заметные. Пока зажат `Ctrl/Cmd`, клик по выбранному неактивному сегменту только переносит на него фокус и сохраняет группу. Повторный `Ctrl/Cmd + клик` по текущему сегменту снимает выбор только с него и возвращает фокус предыдущему активному сегменту. Начало перетаскивания или изменения размера также назначает соответствующий сегмент текущим без разрушения группы.
- Маркер перемещения всегда находится снаружи слева от сегмента, в том числе у левого края страницы. Он не перекрывает текст и может выходить за границу листа.
- Синий маркер в правом нижнем углу изменяет размер сегмента. Он всегда находится снаружи рамки, в том числе у правого и нижнего края страницы, поэтому не перекрывает содержимое.
- Размер фиксируется при отпускании кнопки мыши, в том числе если браузер завершил захват указателя вне самого маркера. `Esc` по-прежнему отменяет незавершённое изменение.
- Положение и размеры сегмента изменяются непосредственно на макете: перетаскиванием внешнего маркера слева и маркера размера справа снизу.
- Нажатие `Esc` отменяет незавершённое перетаскивание или изменение размера, снимает выделение, закрывает открытые окна и убирает фокус.

### Сетка

| Элемент | Что делает |
| --- | --- |
| Список с иконкой сетки | Контрол шириной 72 px выбирает плотность квадратной сетки: `XS`, `SM`, `MD`, `LG`, `XL` или `XXL`. Между контролом и разделителем оставлен внутренний отступ. `XS` даёт самую мелкую сетку, `XXL` — самую крупную. Все перемещаемые сегменты всегда привязаны к выбранной сетке. |

Размер квадратов рассчитывается относительно ширины рабочей области каждой страницы, поэтому интерфейс не требует подбирать пиксели для документов разного формата. По горизонтали рабочая область всегда делится на целое число одинаковых квадратов; вертикальные и горизонтальные линии начинаются от одного левого верхнего угла. Если высота не делится на целый квадрат, нижняя граница остаётся отдельной доступной линией привязки. Сетка влияет только на координаты расстановки. Она не округляет ширину или высоту сегмента и не изменяет результат команд подгонки по содержимому.

Во время перетаскивания сегмент движется свободно. После отпускания мыши его левый верхний угол подтягивается к ближайшему пересечению сетки. Возле правой и нижней границ выбирается последняя доступная ячейка, на которой весь сегмент остаётся внутри рабочей области; его размеры при этом не меняются.

Зелёная рамка обозначает **рабочую область документа**, а не границы найденного текста. Лист имеет минимальную высоту формата A4: короткий исходник не сжимает страницу и не растягивается, а оставшееся место становится свободной рабочей областью. Более высокие исходные страницы сохраняют свою высоту. Рабочая область рассчитывается одинаково для страниц одного формата и оставляет симметричные поля по всем сторонам. Сегменты, включая первоначально распознанные, автоматически ограничиваются этой областью и не могут находиться в полях. При экспорте свободные зоны сохраняются как поля DOCX/PDF.

Нижняя ручка зелёной рамки изменяет высоту рабочей области текущей страницы. Граница привязывается к сетке и не может пройти выше уже расположенных сегментов. После уменьшения области выравнивание относительно документа и «Расстановка» используют её новую высоту. Растяните границу вниз, чтобы снова использовать полную высоту листа; изменение можно отменить через `Ctrl/Cmd + Z`.

## 8. Форматирование текста

Если внутри активного сегмента выделен фрагмент текста, шрифт, цвет, жирность, курсив и размер применяются только к этому фрагменту. Без текстового выделения и выключенного пакетного режима форматирование применяется к последнему выбранному сегменту. В нижней строке карточки сначала расположена кнопка **«Выбрать все сегменты»**, затем чекбокс **«Применить ко всем выбранным»**. Чекбокс распространяет действие всей панели только на текущую группу выделения, а не на остальные сегменты документа.

Список шрифтов содержит `Arial`, `Aptos`, `Calibri`, `Cambria`, `Times New Roman`, `Georgia`, `Verdana`, `Tahoma` и `Courier New`. Цвет выбирается компактной цветовой кнопкой. Word сохраняет название выбранного семейства, но на компьютере получателя этот шрифт должен быть установлен или доступен как облачный шрифт Office; иначе Word выполнит подстановку, способную изменить переносы строк. Для наиболее переносимого результата рекомендуется Arial или Times New Roman.

| Кнопка или поле | Что делает |
| --- | --- |
| **B** | Включает или выключает полужирное начертание. |
| **I** | Включает или выключает курсив. |
| **⇤** | Выравнивает текст по левому краю. |
| **≡** | Центрирует текст внутри сегмента. |
| **⇥** | Выравнивает текст по правому краю. |
| **☰** | Выравнивает текст по ширине. |
| **Размер шрифта, px: − / размер / +** | Уменьшает или увеличивает размер выделенного текста либо выбранных сегментов с шагом 1 px. Значение от 10 до 80 можно ввести непосредственно в поле. Символ `≠` означает, что у выбранных сегментов разные размеры; подсказка раскрывает это состояние текстом. Ввод числа заменяет смешанные значения одним размером. `N/A` не используется, поскольку обычно означает «не применимо» или «нет данных», а размеры в данном случае известны, но различаются. |
| **Высота строки: − / значение / +** | Работает так же, как размер шрифта: кнопки изменяют высоту строки с шагом 0,05 в диапазоне 0,8–3, а поле принимает прямой ввод. Подпись и сам контрол располагаются в одну строку без переноса. При разных значениях отображается `≠`. |
| **Выбрать все сегменты / Снять выбор со всех** | Первое нажатие выделяет во всём документе все активные сегменты, для которых доступна настройка типографики. Кнопка получает активное состояние и меняет подпись; повторное нажатие снимает весь выбор. |
| **Применить ко всем выбранным** | Распространяет семейство и цвет шрифта, размер, высоту строки, жирность, курсив или выбранное выравнивание на все сегменты текущего выделения. Внутренние индивидуальные значения соответствующего свойства заменяются общим. |

В режиме «Макет» инспектор содержит отдельный блок **«Типографика»**. Шрифт, размер шрифта и высота строки расположены в одном горизонтальном ряду. Цвет находится в строке «Начертание, выравнивание и цвет» рядом с кнопками жирности, курсива и выравнивания. Поля `X`, `Y`, «Ширина» и «Высота» удалены: геометрия управляется мышью непосредственно на макете.

Команды выравнивания текста по левому краю, центру, правому краю и ширине изменяют только расположение текста внутри рамки. Они не запускают подгонку геометрии и поэтому сохраняют заданные пользователем ширину, высоту и координаты сегмента, в том числе после растяжения на рабочую область.

До ручного изменения размера каждый сегмент всегда подгоняется по ширине и высоте текста. Это происходит при загрузке документа и после изменения текста, перевода или типографики. Ручное изменение рамки фиксирует затронутый размер, после чего автоматика его не перезаписывает. Команды размера по содержимому снимают соответствующую ручную фиксацию; растяжение на рабочую область и минимальная ширина считаются ручным выбором размера. Все сегменты всегда остаются внутри рабочей области документа.

Сначала рамка растёт по ширине без лишнего переноса строки. Если строка физически шире рабочей области, ширина ограничивается её границей, и только тогда текст переносится на следующие строки. Перенос выполняется только между словами: одно слово никогда не разбивается на несколько строк, в том числе в экспортированном документе. Цвет и выравнивание текста геометрию не меняют. Автоподстройка не изменяет размеры остальных сегментов.

## 9. Перевод всего документа

Аккордеон **«Перевод всего документа»** всегда доступен в инспекторе независимо от наличия выбранного сегмента. Он содержит выбор языка исходника и языка перевода, общую инструкцию для AI, сохранённые инструкции, режим использования БЗ, массовый выбор сегментов и кнопку перевода. Выделение объектов на макете синхронизировано с пакетом перевода: один выделенный переводимый сегмент даёт счётчик `1`, множественное выделение — соответствующее число. Блок массового выбора переводимых сегментов находится непосредственно над кнопкой перевода; **«Выбрать все»** и **«Снять выбор»** меняют выделение на макете и пакет одновременно.

Порядок чтения сегментов автоматически пересчитывается сверху вниз и слева направо при каждом сохранении сцены. Ручные координаты, размеры, тексты и переводы при этом не изменяются. Отдельной кнопки проверки структуры нет; полную проверку ошибок и предупреждений выполняет команда **«Финальная проверка макета»**.

| Кнопка | Что делает |
| --- | --- |
| **Выбрать все** | Отмечает все строки для массового перевода. Слева в той же строке показан белый акцентный счётчик «Выбрано: N из M сегментов», справа — приглушённая подпись флажка. У флажка только два состояния: он включён, когда выбраны все переводимые сегменты, и выключен, когда выбрана только часть сегментов или не выбран ни один. |
| **Снять выбор** | Снимает отметку сразу со всех выбранных сегментов. Кнопка расположена рядом с «Выбрать все» и недоступна, если ни один сегмент не выбран. |
| **Пересегментация макета** | Красная кнопка находится последней в аккордеоне «Тестирование» и отделена верхним отступом от кнопок проверки. Повторно отправляет сохранённые страницы выбранному AI-провайдеру и полностью заменяет текущую сцену новым набором сегментов. Новые сегменты получают автоматический размер по тексту и ограничиваются рабочей областью документа. Перед запуском открывается окно подтверждения с предупреждением: все переводы сегментов, инструкции, ручные правки, стили и изменения расположения будут потеряны. На текущем этапе резервная версия не создаётся; «Отмена», закрытие окна и `Esc` прекращают действие без изменений. |
| **Перевести выбранные** | Основная акцентная кнопка переводит отмеченные сегменты. Если отмечены все, надпись меняется на «Перевести весь документ». |
| **Проверить и исправить макет** | Рендерит текущий макет и сравнивает его с оригиналом выбранной моделью. Уверенные изменения координат и типографики применяются автоматически. Предложения изменить ширину или высоту сохраняются только как рекомендации и не перезаписывают размер по тексту или размер, зафиксированный пользователем. Во время задания эта же кнопка меняется на «Отменить проверку макета» и прерывает выполняющееся AI-сравнение. Отдельная кнопка отмены не появляется, поэтому компоновка панели остаётся стабильной. |
| **Финальная проверка макета** | Запускает локальную проверку готовности документа без обращения к AI и без автоматического изменения сегментов. Показывает найденные ошибки и предупреждения, по которым можно перейти к связанным объектам. |

Машинный перевод всегда требует проверки переводчиком.

Выбор сегментов выполняется непосредственно на макете: обычный клик выбирает один объект, `Ctrl/Cmd + клик` и рамочное выделение формируют группу. Счётчик и пакет перевода обновляются сразу. Частичный общий выбор не добавляет третье, промежуточное состояние: общий флажок остаётся снятым.

Над каждой парой «Распознанный исходник / Перевод» находится строка сведений о сегменте с фиксированной сеткой из двух равных колонок. Заметка агента занимает левую колонку над оригиналом: активная заметка показывается на жёлтой плашке с красным значком восклицания слева от текста, а при отсутствии заметки сохраняется пустая жёлтая плашка без текста и значка. Уверенность распознавания и доступный для изменения «Тип содержимого» всегда находятся в правой колонке над переводом. Выпадающий список типа открывается независимо от режима выделения на холсте, а изменение сразу применяется к этому сегменту. Благодаря постоянной плашке заметки уверенность и тип не смещаются. Под парой текстовых полей расположена широкая панель инструментов сегмента на всю ширину двух колонок; в неё вынесены локальная AI-инструкция и первоначальный вариант перевода AI, когда он доступен. Правый `object-inspector` пока остаётся в интерфейсе, но для этих основных действий обращаться к нему не требуется.

## 10. Комментарии и сохраненные инструкции для AI

### Инструкция для AI

Поле «Инструкция для AI» задаёт правило для выбранных сегментов или всего документа. Его минимальная высота равна исходной высоте карточки с колонкой действий: поле можно увеличить вертикальным ресайзом, но нельзя уменьшить ниже 96 px. Примеры:

- `Все имена передавай транслитерацией.`
- `Сохраняй номера и даты без изменений.`
- `Все печати не включай в финальную сборку.`

| Кнопка | Что делает |
| --- | --- |
| **Использовать инструкцию** | Добавляет выбранную сохраненную инструкцию в поле «Инструкция для AI». Уже добавленный одинаковый текст не дублируется. |
| **Сохранить инструкцию в список инструкций** | По явному действию сохраняет текущую инструкцию в общий набор шаблонов. Кнопка недоступна, пока поле инструкции пусто или содержит только пробелы. |
| **Редактировать инструкцию** | Открывает текст выбранной готовой инструкции для изменения. |
| **Сохранить изменения** | Сохраняет новый текст шаблона на сервере. |
| **Отмена** | Закрывает редактор шаблона без сохранения. |
| **Удалить инструкцию из списка инструкций** | Удаляет выбранный шаблон после подтверждения. Уже применённые к документам комментарии не изменяются. |
| **Исправить выбранные сегменты с учетом инструкции** | Повторно отправляет отмеченные переводы AI с учётом общей инструкции и локальных комментариев. По умолчанию кнопка отключена и становится доступной, когда выбраны сегменты и задана применимая общая инструкция или локальный комментарий. |
| **Исправить весь документ с учетом инструкции** | Повторно обрабатывает все уже переведённые сегменты документа. По умолчанию кнопка отключена и становится доступной, когда в документе есть сегменты и задана общая инструкция либо хотя бы один локальный комментарий. |

Все действия в карточке «Инструкция для AI» показаны кнопками только с иконками. Справа от поля инструкции в вертикальной колонке расположены «Сохранить инструкцию в список инструкций», «Исправить весь документ с учетом инструкции» и «Исправить выбранные сегменты с учетом инструкции». «Использовать инструкцию», «Редактировать инструкцию» и «Удалить инструкцию из списка инструкций» находятся в одной строке справа от списка сохраненных инструкций. При наведении или клавиатурном фокусе общий компонент тултипа показывает полное название команды из её доступного имени; все тултипы начинаются с заглавной буквы. Это же поведение применяется ко всем иконным кнопкам редактора. Тултип сначала пытается разместиться по центру над кнопкой, а при нехватке места выбирает свободную сторону — снизу, справа или слева — и не выходит за границы окна.

Список сохраненных инструкций хранится на сервере и доступен в других документах. Одинаковые инструкции повторно не создаются.

### Отдельное окно «AI-инструкции»

Окно открывается одноимённой кнопкой в верхней панели и позволяет управлять всем набором независимо от документа.

| Кнопка или поле | Что делает |
| --- | --- |
| Поиск | Фильтрует список по полному тексту инструкции. |
| **＋ Новая инструкция** | Открывает пустую форму создания шаблона. |
| **Изменить** | Открывает выбранную инструкцию и позволяет изменить её текст. |
| **Сохранить инструкцию** | Создаёт новый шаблон или сохраняет изменения существующего. |
| **Отмена** | Закрывает форму без сохранения. |
| **Удалить** | Удаляет шаблон после подтверждения. Комментарии, ранее скопированные в документы, не изменяются. |
| **×** | Закрывает окно управления инструкциями. |

### Комментарий отдельного сегмента

В аккордеоне инспектора «Посегментный перевод» находятся:

| Кнопка | Что делает |
| --- | --- |
| **Добавить** | Добавляет выбранный готовый шаблон к комментарию конкретного сегмента. |
| **Сохранить** | Сохраняет локальный комментарий как новый готовый шаблон. |
| **Исправить ИИ** | Исправляет только этот перевод. Локальный комментарий имеет приоритет над общим. |

Если AI по явной инструкции пометил объект как исключённый, объект сохраняется в проекте, но не попадает в DOCX/PDF. Такое действие можно отменить через историю.

## 11. Инспектор выбранного сегмента

Инспектор содержит пять независимых аккордеонов. Все они видимы и раскрыты по умолчанию и могут сворачиваться отдельно, без изменения состояния остальных. Каждый раскрытый раздел занимает полную высоту своего содержимого; при нехватке высоты прокручивается весь инспектор, а элементы разделов не сжимаются и не обрезаются. «Перевод всего документа» и «Тестирование» доступны без выбора сегмента; над всеми аккордеонами при отсутствии выбора показывается компактное уведомление, а содержимое трёх разделов редактирования объекта остаётся приглушённым и неактивным до выбора сегмента. Заголовки этих разделов при этом остаются доступными, поэтому аккордеоны не блокируются.

| Аккордеон | Содержимое |
| --- | --- |
| **Перевод всего документа** | Языки, общая инструкция и сохранённые инструкции, режим БЗ, массовый выбор и запуск перевода. Вложенного заголовка и видимого текстового статуса агента нет. |
| **Посегментный перевод** | Тип и уверенность распознавания, исходник, перевод, заметка агента, локальная инструкция для AI, сохранённые инструкции, первоначальный вариант AI, внутренние переводческие единицы, База знаний и действия с сегментом. Локальная AI-корректировка доступна при выборе одного сегмента; при групповом выборе инспектор просит выбрать один. |
| **Типографика** | Шрифт, размер, высота строки, начертание, выравнивание текста, цвет и область применения форматирования. |
| **Расположение** | Карточки «Сегменты» и «Расположение сегментов внутри рабочей области»: подгонка размеров, объединение, устранение наложений, возврат геометрии, выравнивание и расстановка. |
| **Тестирование** | Команды «Проверить и исправить макет» и «Финальная проверка макета», а также состояние последнего AI-сравнения. Опасная «Пересегментация макета» расположена внизу с отдельным верхним отступом. |

Заголовок выбранного сегмента в инспекторе не показывается. Над текстовыми полями в одной строке находятся «Тип содержимого» шириной не более 180 px и «Уверенность распознавания». Уверенность оформлена информационной плашкой: название слева занимает две строки и использует ту же служебную типографику, что и «Тип содержимого», а процент расположен справа и выделен акцентным цветом. Поля «Распознанный исходник» и «Перевод / обозначение» расположены в следующем ряду и занимают по 50% ширины; минимальная высота каждого поля — 160 px, увеличить её можно вручную. Заметка агента показывается под текстовыми полями.

Все полноразмерные и компактные информационные плашки используют единый компонент **«Заметка»**. Доступны нейтральный, информационный, успешный, предупреждающий и ошибочный варианты; одинаковый смысл во всех разделах приложения имеет одинаковые цвет, рамку и типографику. Эталонные варианты показаны на странице **«Компоненты»**.

| Элемент | Назначение |
| --- | --- |
| **Тип содержимого** | Меняет тип объекта: текст, таблица, ячейка, штамп, печать, подпись, рукописный текст, логотип, изображение или неопределённый объект. |
| **ID таблицы / строка / колонка / размеры объединения** | Задаёт структурную позицию ячейки в настоящей табличной модели. |
| **Распознанный исходник** | Позволяет исправить результат распознавания. |
| **Уверенность распознавания** | Показывает оценку модели. Низкое значение требует ручной проверки по оригиналу. |
| **Перевод / обозначение** | Содержит выбранный перевод или служебное обозначение объекта. |
| **Шрифт, px / Высота строки** | Задаёт размер текста и межстрочный коэффициент выбранного объекта. |

Формат специальных объектов при переводе:

- печать — `/Печать: распознанный текст/`;
- штамп — `/Штамп: распознанный текст/`;
- подпись — `/Подпись/`.

## 12. Части сегмента и отдельные термины

Внутренние части позволяют отдельно перевести слово или фразу, не создавая новый прямоугольник на странице.

| Кнопка | Что делает |
| --- | --- |
| **Разбить по предложениям** | Создаёт несколько внутренних переводческих единиц внутри одного внешнего сегмента. |
| **Вынести выделенное в отдельную часть** | Делает выделенное в поле исходника слово или фразу самостоятельной внутренней единицей. |
| **Объединить внутри** | Снова соединяет внутренние единицы, не меняя геометрию внешнего блока. |
| **Применить все 100% совпадения** | Подставляет все точные предложения Базы знаний во внутренние единицы выбранного сегмента. |
| **Сохранить эту пару в БЗ** | Сохраняет конкретную внутреннюю пару «исходник → перевод». |
| **Применить 100% совпадение** | Применяет найденный точный перевод только к соответствующей части. |

Если разбиение выполняется после ввода переводов, приложение предупреждает о возможной очистке несопоставленных частей.

## 13. Размеры и расположение

### Сегменты

Карточка «Сегменты» объединяет команды изменения геометрии, объединения и исправления пересечений выбранных блоков.

| Кнопка | Что делает |
| --- | --- |
| Кнопка с горизонтальными стрелками | Подбирает ширину выбранных сегментов по содержимому. Полное название «Ширина по тексту» доступно в подсказке. |
| Кнопка с вертикальными стрелками | Подбирает высоту выбранных сегментов по содержимому. Полное название «Высота по тексту» доступно в подсказке. |
| Кнопка с четырьмя диагональными стрелками | Снимает ручную фиксацию обеих осей и возвращает постоянную автоматическую подгонку ширины и высоты по тексту. Полное название «Оба размера по тексту» доступно в подсказке. Если результат не помещается у правого или нижнего края, блок сдвигается и ограничивается рабочей областью. Для одинакового текста и оформления помогает получить одинаковые размеры блоков. |
| Кнопка с горизонтальными стрелками между границами | Растягивает каждый выбранный сегмент на всю ширину его рабочей области и совмещает левую границу сегмента с левой зелёной границей. Высота сегмента при этом не уменьшается. |
| Кнопка с вертикальными стрелками между границами | Растягивает каждый выбранный сегмент точно на всю высоту его рабочей области, совмещает верхнюю границу сегмента с верхней зелёной границей и фиксирует выбранную высоту. |
| Кнопка со сходящимися горизонтальными стрелками | Уменьшает ширину до CSS-эквивалента `min-content`: ориентиром служит самое длинное неделимое слово или фрагмент. Высота автоматически увеличивается под появившиеся переносы, поэтому содержимое остаётся полностью видимым. |
| Кнопка с двумя соединёнными скрепками | Объединяет минимум два выбранных сегмента одной страницы. Полное название «Объединить выбранные сегменты» доступно в подсказке. |
| Кнопка с расходящимися прямоугольниками | Выполняет детерминированное разнесение пересекающихся блоков без обращения к AI. Полное название «Исправить наложения» доступно в подсказке. |
| Кнопка с рамкой и возвратной стрелкой | Возвращает выбранные сегменты к сохранённым исходным координатам и размерам. Полное название «Вернуть в исходное положение в документе» доступно в подсказке. |

Перетаскивание изменяет только координаты выбранных сегментов. Оно не пересчитывает размеры перемещаемого или соседних блоков. Повторная подгонка одного сегмента также не изменяет размеры ранее нормализованных сегментов.

Высота сегмента не может быть меньше высоты его содержимого, пока содержимое физически помещается в оставшуюся рабочую область. При уменьшении ширины перенос строк пересчитывается, и минимальная высота увеличивается автоматически, но рамка никогда не выходит за зелёную границу. Если текст невозможно вместить без выхода за рабочую область, финальная проверка сообщает о переполнении.

### Выравнивание выбранных относительно друг друга

Работает только для сегментов одной страницы.

Команды разделены на две подписанные группы: «Выровнять друг относительно друга, X» для горизонтального положения и «Выровнять друг относительно друга, Y» для вертикального положения.

| Кнопка | Что делает |
| --- | --- |
| **Лево / Центр X / Право** | Выравнивает выбранные блоки по горизонтальной стороне или оси общей группы. |
| **Верх / Центр Y / Низ** | Выравнивает выбранные блоки по вертикальной стороне или оси общей группы. |
Кнопки получают активный голубой фон, когда выбранные сегменты фактически соответствуют указанному выравниванию. Состояние пересчитывается после ручного перемещения, изменения размера и отмены действия.

### Выравнивание относительно док-та

Команды также разделены по осям: «Выравнивание относительно док-та, X» содержит «Лево», «Центр X» и «Право», а «Выравнивание относительно док-та, Y» — «Верх», «Центр Y» и «Низ». Они перемещают всю выбранную группу относительно рабочей области документа. Защитные поля страницы остаются недоступными для размещения.

Активная кнопка в этой группе показывает фактическое положение всей выбранной группы относительно рабочей области документа.

### Расстановка

Расстановка размещает выбранные сегменты вдоль одной оси внутри рабочей области документа:

- секция «Направление, X» содержит прямые кнопки «Лево», «Центр X», «Право», «Между», «Вокруг» и «Равномерно»;
- секция «Направление, Y» содержит прямые кнопки «Верх», «Центр Y», «Низ», «Между», «Вокруг» и «Равномерно»;
- «Между» распределяет свободное место только между сегментами, «Вокруг» оставляет равные поля вокруг них, а «Равномерно» делает одинаковыми промежутки и внешние поля;
- положение сегментов по второй оси при расстановке не меняется;
- если суммарный размер сегментов больше доступной длины выбранной оси, программа пропорционально уменьшает их размеры и текст, а затем выполняет расстановку вместо остановки с ошибкой;
- выпадающих списков и отдельной кнопки применения нет: нажатие любой кнопки расстановки выполняет действие сразу.

## 14. База знаний

База знаний хранит утверждённые переводы и глоссарии. Записи сохраняются в нижнем регистре, а при применении регистр автоматически адаптируется к исходнику: нижний, первая заглавная или полностью прописной.

### В интерфейсе сегментов

Режим использования Базы знаний выбирается в главном блоке работы с сегментами сразу после карточки «Инструкция для AI». Поиск похожих записей, выбор глоссария и работа с найденными совпадениями остаются в инспекторе выбранного сегмента.

| Кнопка или поле | Что делает |
| --- | --- |
| **Найти похожие** | Ищет точные и векторно похожие записи для активной части выбранного сегмента. |
| **Использовать** | Подставляет выбранное предложение БЗ. |
| **Открыть БЗ** | Открывает общий редактор записей. |
| Список глоссариев | Выбирает активный глоссарий документа. |
| **＋ Новый** | Создаёт новый глоссарий. |
| **Только подсказки** | Показывает совпадения, но не подменяет самостоятельный перевод AI. |
| **БЗ имеет приоритет** | Использует точные и достаточно уверенные совпадения, сохраняя первоначальный вариант AI рядом. |
| **Добавить переведённые единицы в БЗ** | Сохраняет заполненные пары выбранных сегментов; существующие точные пары не перезаписываются молча. |

Над сегментом макета показывается жёлтая плашка **«БЗ · N»**, где `N` — число найденных совпадений. Нажатие на плашку открывает найденные варианты с процентом совпадения и позволяет применить нужный перевод.

После создания, изменения или удаления записи БЗ открытый документ автоматически пересчитывает совпадения и сразу обновляет подсказки и подсветку — запускать повторный перевод не нужно. При каждом открытии любого сохранённого документа приложение заново сверяет его сегменты с актуальной БЗ: новые вложенные и полные совпадения получают плашку **«БЗ · N»**, а удалённые записи исчезают. Если перевод ранее был применён из БЗ, его текст сохраняется как ручной результат и не удаляется. В режиме **«Только подсказки»** новый вариант не перезаписывает уже введённый перевод: пользователь применяет его явно через кнопку **«Использовать»**.

| Кнопка во всплывающем окне | Что делает |
| --- | --- |
| **Использовать** | Применяет выбранный вариант к переводу. |
| **Открыть в БЗ** | Открывает соответствующую запись для просмотра или редактирования. |
| **×** | Закрывает список предложений и очищает его содержимое. Список также можно закрыть клавишей `Escape` или нажатием за его пределами. |
| **Использовать перевод ИИ** | Возвращает первоначальный AI-вариант после применения приоритетной БЗ. |

### Общее окно Базы знаний

| Кнопка | Что делает |
| --- | --- |
| **Найти** | Ищет по исходному тексту и переводу с учётом фильтра глоссария. `Enter` в поиске выполняет ту же команду. |
| **＋ Новая запись** | Открывает форму новой пары. |
| **Изменить** | Открывает существующую запись в форме. |
| **Удалить** | Удаляет запись после подтверждения. Существующие документы не изменяются. |
| **Сохранить запись** | Создаёт запись или сохраняет изменения. При изменении исходника пересчитывается embedding. |
| **Отмена** | Закрывает форму без сохранения. |
| **← Назад / Далее →** | Переключает страницы длинного списка. |
| **×** | Закрывает Базу знаний. |

## 15. Операции с объектами

| Кнопка | Что делает |
| --- | --- |
| **Объединить выбранные сегменты** | Находится в карточке «Сегменты» раздела «Расположение». Объединяет несколько сегментов одной страницы в один блок, сохраняя общий порядок текста и стили. |
| **Вынести текст в отдельный блок** | Переносит выделенный текст в новый геометрический сегмент. Если выделения нет, делит по позиции курсора. |
| **Вернуть исходное положение** | Возвращает выбранные сегменты к сохранённым исходным координатам и размерам. |
| **Исключить из сборки** | Скрывает выбранные объекты из рабочего экспорта. Действие можно отменить. |

## 16. Финальная проверка макета

Кнопка «Финальная проверка макета» находится в аккордеоне «Тестирование». Панель проверки выезжает справа поверх редактора с той же плавной анимацией, что и инспектор. Она занимает всю высоту рабочей области под панелью инструментов и не сужает макет, панель оригинала или инспектор. Панель показывает количество ошибок, предупреждений и готовых переводов. Проверяются:

- отсутствующие переводы;
- низкая уверенность распознавания — менее 76%;
- отсутствие привязки объекта к странице;
- выход за физические границы страницы;
- размещение в защитных полях вне рабочей области;
- вероятное переполнение текстом на основе размера шрифта, ширины и высоты сегмента;
- заметные пересечения сегментов;
- пустые объекты, которые не попадут в экспорт.

Исключённые из сборки объекты в отчёт не входят. Проверка ничего не исправляет и сама по себе не запускает экспорт: это контрольный список перед скачиванием файла. Нажатие на предупреждение выбирает связанные сегменты и переносит редактор к их странице. После ручного исправления нажмите кнопку с иконкой обновления справа от счётчиков: программа сохранит сцену и заново выполнит проверку выбранного сегмента или группы. Отдельное уведомление не показывается — обновляются общий отчёт и его счётчики. Без выбранного сегмента кнопка недоступна. Строка счётчиков и кнопка имеют одинаковую высоту 44 px. Кнопка **×** закрывает панель.

## 17. Документы и архив

| Кнопка | Что делает |
| --- | --- |
| **Открыть** | Открывает рабочий документ в отдельной вкладке. |
| **В архив** | Убирает документ из активного списка, сохраняя его файлы и сцену. |
| **Восстановить / Вернуть** | Возвращает архивный документ в работу. |
| **Удалить** | Окончательно удаляет исходник, сцены и результаты после подтверждения. Операция необратима. |
| **×** | Закрывает окно документов. |

## 18. Настройка AI-провайдера

| Кнопка или поле | Что делает |
| --- | --- |
| **Провайдер** | Выбирает единый AI-маршрут: AITunnel или Codex. Распознавание, повторный анализ, перевод и AI-проверка идут по выбранному маршруту. |
| **Модель AITunnel** | Выбирает доступную модель. Для анализа документов нужна модель с поддержкой изображений. |
| **API-ключ AITunnel** | Принимает ключ провайдера. Сохранённое значение обратно в браузер не возвращается. |
| **Сохранить ключ на сервере после перезапуска** | Разрешает записать ключ в серверный `.env`, исключённый из Git. |
| **Проверить подключение** | Проверяет ключ, каталог и доступность выбранной модели без запуска полного анализа документа. |
| **Сохранить** | Сохраняет выбранный маршрут и модель; при явной настройке сохраняет ключ. |
| **Удалить ключ** | Удаляет ключ из памяти процесса и серверного `.env`. |
| **×** | Закрывает настройки. |

Ключ передаётся из браузера на backend в RSA-OAEP-шифротексте. Backend обращается к AITunnel только по HTTPS. Сами изображения документа при AI-анализе отправляются выбранному внешнему провайдеру.

## 19. Сохранение данных

- Сцена документа автоматически сохраняется после редактирования.
- Готовые документы восстанавливаются после перезагрузки страницы.
- Незавершённые задания сохраняются и возобновляются после перезапуска backend.
- База знаний хранится в PostgreSQL/pgvector, если задан `DATABASE_URL`.
- Сохраненные AI-инструкции хранятся на сервере в `data/studio/translation-instructions.json`.
- Для production каталогу `data/studio` и PostgreSQL требуется постоянное хранилище и резервное копирование.

## 20. Горячие клавиши и управление

| Комбинация | Действие |
| --- | --- |
| `Ctrl/Cmd + Z` | Отменить последнее действие, не сбрасывая выбранные сегменты и текущий фокус группы. |
| `Ctrl/Cmd + Shift + Z` | Повторить отменённое действие с сохранением выделения и фокуса. |
| `Ctrl/Cmd + клик` | Добавить новый сегмент в группу; перенести фокус на уже выбранный неактивный сегмент; снять выбор с текущего сегмента и вернуть предыдущий фокус. |
| `Ctrl/Cmd + колесо` | Плавный зум области под курсором. |
| Зажатая левая кнопка на оригинале | Перемещение области просмотра оригинального документа. |
| `Enter` в поиске БЗ | Запустить поиск. |
| `Enter` в поле единого размера | Применить размер шрифта всему документу. |
| `Esc` | Закрыть всплывающие окна, снять выделение и фокус. |

## 21. Важные ограничения

- AI может ошибаться в тексте, типе объекта и переводе. Для юридических документов обязательна проверка человеком.
- Повторный анализ исходника перестраивает сцену и может заменить ручные изменения.
- Не все сложные изображения и графические объекты пока воспроизводятся в DOCX с полной визуальной точностью.
- Низкоуверенные рекомендации AI по верстке не следует принимать без сравнения с оригиналом.
- Авторизация пользователей и разделение данных между организациями относятся к следующему production-этапу.
